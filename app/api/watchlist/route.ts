import { NextResponse } from "next/server";
import { createSupabaseAdminClient,getCurrentUserProfile } from "@/lib/supabase/server";
export async function GET(){const {user}=await getCurrentUserProfile();if(!user)return NextResponse.json({items:[]});const db=createSupabaseAdminClient();const {data,error}=await db.from("watchlist_items").select("*").eq("user_id",user.id).order("created_at",{ascending:false});return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({items:data||[]})}
export async function POST(r:Request){const {user}=await getCurrentUserProfile();if(!user)return NextResponse.json({error:"Please sign in."},{status:401});const b=await r.json(),address=String(b.address||"").trim();if(!address)return NextResponse.json({error:"Address required."},{status:400});const db=createSupabaseAdminClient();const payload={user_id:user.id,email:user.email||"",address,target_price:Number(b.target_price||0),note:b.note||""};const {data,error}=await db.from("watchlist_items").upsert(payload,{onConflict:"user_id,address"}).select("id").single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({ok:true,id:data.id})}
export async function DELETE(r:Request){const {user}=await getCurrentUserProfile();if(!user)return NextResponse.json({error:"Please sign in."},{status:401});const id=new URL(r.url).searchParams.get("id");const db=createSupabaseAdminClient();const {error}=await db.from("watchlist_items").delete().eq("id",id).eq("user_id",user.id);return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({ok:true})}
